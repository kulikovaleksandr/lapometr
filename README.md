# 🐾 Лапометр

Веб-приложение для учёта заботы о домашних питомцах с геймификацией и соревновательным элементом.

## ✨ Основные возможности

### 📊 Учёт активностей
- Журнал всех действий по уходу за питомцем
- Система лапок (баллов) за каждую активность
- Отслеживание прогресса и статистики

### 👥 Мультипользовательность
- Несколько хозяев для одного питомца
- Система ролей (владелец/помощник)
- Возможность отмечать активности "от имени" другого хозяина

### 🏆 Дуэль хозяев
- Соревнование между хозяевами по количеству лапок
- Периоды: сегодня, неделя, месяц, сезон, всё время
- История победителей и чемпион года

### 📅 Сезонная механика
- **Автоматическое обнуление лапок** каждый месяц
- Настройка дня обнуления (по умолчанию 1-е число)
- Чип с информацией о текущем сезоне и днях до конца
- «Кубок» с историей победителей по месяцам

#### 🎯 Краевые случаи (реализовано)

**Питомец создан в середине месяца**
Если питомец создан не в начале месяца, сезон начинается с даты создания питомца, а не с 1-го числа месяца. Это обеспечивает справедливый учёт с самого начала.

*Пример:* Питомец создан 15-го июня → сезон длится с 15-го июня по 30-е июня.

**Хозяин вышел из питомца посреди сезона**
Если хозяин покидает питомца в середине сезона, его результаты за этот сезон сохраняются, но с пометкой о бывших хозяевах. Это обеспечивает прозрачность истории.

*Пример:* Хозяин А участвовал в сезоне, затем покинул питомца. При закрытии месяца результат сохраняется с пометкой: "Результат включает данные бывших хозяев: 1 чел."

### 🎯 Краевые случаи (реализовано)

#### Питомец создан в середине месяца
Если питомец создан не в начале месяца, сезон начинается с даты создания питомца, а не с 1-го числа месяца. Это обеспечивает справедливый учёт с самого начала.

**Пример:** Питомец создан 15-го июня → сезон длится с 15-го июня по 30-е июня.

#### Хозяин вышел из питомца посреди сезона
Если хозяин покидает питомца в середине сезона, его результаты за этот сезон сохраняются, но с пометкой о бывших хозяевах. Это обеспечивает прозрачность истории.

**Пример:** Хозяин А участвовал в сезоне, затем покинул питомца. При закрытии месяца результат сохраняется с пометкой: "Результат включает данные бывших хозяев: 1 чел."

### 📈 Статистика и аналитика
- Графики по дням, активностям, хозяевам, часам
- Регулярность выполнения активностей
- Доли лапок по хозяевам

### 🏥 Здоровье питомца
- Ветеринарный календарь
- Автографик прививок по виду и возрасту
- Отслеживание веса с расчётом дозировок
- Учёт расходов по категориям

### 🎮 Геймификация
- 15 ачивок 4 уровней редкости
- Еженедельные челленджи
- Ежедневные цели с прогресс-кольцом
- Уровни заботы (от «Знакомство» до «Лапа-легенда»)

### 🔔 Напоминания
- Браузерные уведомления
- Telegram-бот
- Серверные напоминания (Edge Functions)

### 📱 PWA
- Установка на устройство
- Работа офлайн
- Push-уведомления

### 🎨 5 тем оформления
- Ночь (основная)
- День
- Латте
- Лес
- Олива

## 🛠 Технологический стек

- **Frontend:** React 18, TypeScript, Vite
- **Стили:** Tailwind CSS v4
- **База данных:** localStorage (локально) + Supabase (облако)
- **Авторизация:** Supabase Auth (Google OAuth, email)
- **Realtime:** Supabase Realtime
- **Storage:** Supabase Storage для фото
- **Мониторинг:** Sentry, Plausible

## 📦 Установка и запуск

### Локальная разработка

```bash
# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev

# Сборка для продакшена
npm run build

# Предпросмотр продакшен-сборки
npm run preview
```

### Переменные окружения

Создайте файл `.env` на основе `.env.example`:

```bash
# Supabase (опционально, для облачного режима)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Мониторинг (опционально)
VITE_SENTRY_DSN=your-sentry-dsn
VITE_PLAUSIBLE_DOMAIN=your-domain.com

# Web Push (опционально)
VITE_VAPID_PUBLIC_KEY=your-vapid-public-key
```

### Настройка мониторинга

Приложение поддерживает два уровня мониторинга:

#### Sentry (отслеживание ошибок)

1. Создайте проект на [sentry.io](https://sentry.io)
2. Скопируйте DSN из Settings → Client Keys
3. Добавьте в `.env`:
   ```bash
   VITE_SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0
   ```
4. Пересоберите приложение

**Что отслеживается:**
- Непойманные исключения JavaScript
- Ошибки React (через ErrorBoundary)
- Критические ошибки с контекстом

#### Plausible Analytics (приватная аналитика)

1. Создайте сайт на [plausible.io](https://plausible.io)
2. Добавьте домен в `.env`:
   ```bash
   VITE_PLAUSIBLE_DOMAIN=your-domain.com
   ```
3. Добавьте скрипт в `index.html`:
   ```html
   <script defer data-domain="${VITE_PLAUSIBLE_DOMAIN}" src="https://plausible.io/js/script.js"></script>
   ```
4. Пересоберите приложение

**Что отслеживается:**
- Просмотры страниц
- Ключевые действия (отметки активностей, создание питомцев)
- Без персональных данных

**Примечание:** Мониторинг работает только в продакшене (`import.meta.env.PROD`).

### Настройка Web Push уведомлений

Приложение поддерживает Web Push уведомления через VAPID для отправки уведомлений даже при закрытой вкладке.

#### 1. Генерация VAPID ключей

```bash
npm install -g web-push
web-push generate-vapid-keys
```

#### 2. Настройка Supabase

1. Выполните миграцию `supabase/migrations/005_push_subscriptions.sql`
2. Добавьте секреты в Supabase Dashboard → Settings → Secrets:
   ```
   VAPID_PUBLIC_KEY=...
   VAPID_PRIVATE_KEY=...
   VAPID_SUBJECT=mailto:your-email@example.com
   ```
3. Задеплойте Edge Functions:
   ```bash
   supabase functions deploy push-subscribe
   supabase functions deploy push-send
   ```

#### 3. Настройка клиента

Добавьте в `.env`:
```bash
VITE_VAPID_PUBLIC_KEY=your-vapid-public-key
VITE_SUPABASE_FUNCTIONS_URL=https://your-project.supabase.co
```

#### 4. Включение в приложении

В настройках приложения откройте раздел "Push-уведомления" и включите их. Пользователь получит запрос на разрешение уведомлений.

**Особенности:**
- Уведомления работают через Service Worker
- Не требуют открытой вкладки
- Поддерживаются Chrome, Firefox, Edge, Safari 16.4+
- Для iOS требуется добавление сайта на домашний экран

Подробная документация: [`supabase/functions/README.md`](supabase/functions/README.md)

### Серверные напоминания

Edge Function `reminders-send` автоматически собирает просроченные активности и due вет-события, отправляя сводку в Telegram и Web Push **без открытого браузера**.

#### Настройка

**Для платного плана Supabase (с pg_cron):**

1. Включите pg_cron в Supabase Dashboard → Database → Extensions
2. Выполните миграцию `006_pg_cron_reminders.sql`
3. Задеплойте функцию:
   ```bash
   supabase functions deploy reminders-send
   ```

**Для бесплатного плана (внешний cron):**

Используйте GitHub Actions workflow `.github/workflows/reminders-cron.yml`:

1. Добавьте секреты в GitHub → Settings → Secrets:
   - `SUPABASE_URL`
   - `SUPABASE_SERVICE_ROLE_KEY`
2. Workflow запускается автоматически каждый час

Альтернативы: cron-job.org, системный cron (скрипт `supabase/functions/external-cron.sh`)

#### Пример уведомления

```
🐾 Лапометр: пора позаботиться

• Булка: «Покормить» — просрочено на 3 ч
• Булка: «Прививка» — сегодня в 15:00

Отметьте выполнение в журнале!
```

Подробная документация: [`supabase/functions/README.md`](supabase/functions/README.md)

### Подключение облака Supabase

Приложение поддерживает два варианта подключения облака:

#### Вариант A: Через переменные окружения (для продакшена)

Рекомендуется для продакшен-развёртывания. Облако настраивается один раз при сборке.

1. Создайте проект на [supabase.com](https://supabase.com)
2. Выполните миграции из `supabase/migrations/` в SQL Editor
3. Настройте переменные окружения:
   ```bash
   VITE_SUPABASE_URL=https://your-project.supabase.co
   VITE_SUPABASE_ANON_KEY=your-anon-key
   ```
4. Пересоберите приложение: `npm run build`
5. В настройках приложения появится упрощённая форма входа (без полей URL/key)

**Преимущества:**
- Безопасность: ключи не хранятся в браузере пользователя
- Простота: пользователям не нужно настраивать подключение
- Централизация: все пользователи работают с одним проектом Supabase

#### Вариант B: Ручное подключение (для разработки)

Подходит для локальной разработки и тестирования. Каждый пользователь может подключить свой проект Supabase.

1. Создайте проект на [supabase.com](https://supabase.com)
2. Выполните миграции из `supabase/migrations/` в SQL Editor
3. В настройках приложения откройте раздел "Облако и синхронизация"
4. Введите Project URL и Anon key из Settings → API
5. Нажмите "Проверить и подключить"
6. Войдите в аккаунт или зарегистрируйтесь

**Преимущества:**
- Гибкость: можно тестировать с разными проектами Supabase
- Изоляция: данные разных пользователей не пересекаются
- Разработка: удобно для тестирования миграций и настроек

**Примечание:** Если переменные окружения заданы, ручное подключение недоступно.

## 🗄 Структура базы данных

### Локальное хранилище (localStorage)
- `lapometr.db` - основная база данных
- `lapometr.session` - сессия пользователя
- `lapometr.theme` - выбранная тема

### Облачное хранилище (Supabase)
- `users` - пользователи
- `pets` - питомцы
- `pet_owners` - связь питомцев и хозяев
- `activities` - типы активностей
- `logs` - журнал активностей
- `chat_messages` - чат хозяев
- `vet_events` - ветеринарные события
- `weights` - записи веса
- `expenses` - расходы
- `push_subscriptions` - подписки на push-уведомления
- `sync_snapshots` - снапшоты для синхронизации

## 📚 Документация

- [План разработки](ai/plan.md)
- [Архитектура](ai/architecture.md)
- [Задачи](ai/tasks.md)
- [Тестирование](TESTING.md)

## 🔒 Безопасность

- Пароли хэшируются (djb2 для локального режима, bcrypt для облачного)
- RLS-политики в Supabase для защиты данных
- Анти-чит система для лимитов активностей
- Гигиена снапшотов (пароли и email не попадают в облако)

## 🚀 Деплой

### Cloudflare Pages (рекомендуется)
```bash
npm run build
# Загрузите папку dist в Cloudflare Pages
```

### Netlify
```bash
npm run build
# Загрузите папку dist в Netlify
```

### Docker
```bash
docker build -t lapometr .
docker run -p 8080:80 lapometr
```

## 📝 Лицензия

MIT

## 🤝 Вклад

Приветствуется! Создайте issue или pull request.

## 📧 Контакты

Если у вас есть вопросы или предложения, создайте issue в репозитории.
