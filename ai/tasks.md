# Лапометр — задачи

Движемся по этому списку. Статусы: `[x]` готово · `[ ]` в работе/дальше.

## Фаза 0 — Планирование
- [x] 0.1 Собрать требования, зафиксировать MVP-скоуп → `ai/plan.md`
- [x] 0.2 Описать стек, модель данных, механизмы → `ai/architecture.md`
- [x] 0.3 Декомпозиция на задачи → этот файл

## Фаза 1 — Фундамент
- [x] 1.1 `index.html`: заголовок «Лапометр», шрифты Unbounded + Golos Text, favicon-лапка
- [x] 1.2 `src/index.css`: 5 тем (night/day/latte/forest/olive), токены `@theme inline`, keyframes
- [x] 1.3 `lib/types.ts`: доменные типы, уровни заботы
- [x] 1.4 `components/icons.tsx`: рукописный SVG-набор + морды питомцев (кошка/собака/кролик/попугай/хомяк/рыбка)
- [x] 1.5 `components/ui.tsx`: Btn, Modal, Field, Seg, Ring, Bar, CountUp, Avatar, Burst, EmptyState

## Фаза 2 — Аккаунты и профиль
- [x] 2.1 `lib/db.ts`: localStorage-БД, сессия, хэш пароля, uid, даты/plural
- [x] 2.2 Регистрация / вход / выход, инлайн-валидация
- [x] 2.3 Гостевой режим и демо-аккаунт (генератор 30 дней истории, 2 хозяина)
- [x] 2.4 Онбординг: шаг «Хозяин» (имя, цвет, фото)
- [x] 2.5 Синхронизация вкладок через событие `storage`

## Фаза 3 — Питомец и активности
- [x] 3.1 Онбординг: шаг «Питомец» (тип, кличка, порода, дата рождения, цвет)
- [x] 3.2 Стандартные 12 активностей с лапками, лимитами и напоминаниями
- [x] 3.3 CRUD своих активностей (иконка, цвет, лапки, лимиты день/нед/мес, remindH)
- [x] 3.4 Анти-чит: проверка лимитов в `complete()`, UI-статус «2/4 сегодня»

## Фаза 4 — Журнал и лапки
- [x] 4.1 Экран «Журнал»: таймлайн — кружки активностей, соединённые полосой; дата/время/вид/хозяин
- [x] 4.2 Группировка по дням, фильтры по хозяину/активности/периоду
- [x] 4.3 Начисление лапок, прогресс уровня, тост «+N лапок», paw-burst анимация
- [x] 4.4 Модалка «Отметить активность» с лимитами

## Фаза 5 — Хозяева и дуэль
- [x] 5.1 Пригласительный код (показать/копировать/перегенерировать), вход по коду
- [x] 5.2 Экран «Дуэль»: периоды (сегодня/неделя/месяц/всё), корона лидера, лапки, топ-активность, серии
- [x] 5.3 «Гонка 7 дней» — сравнение по дням; вердикт-подколка
- [x] 5.4 Управление хозяевами (удаление, защита от пустого владельца)

## Фаза 6 — Напоминания
- [x] 6.1 `computeDue()`: просрочено / скоро / в норме / никогда
- [x] 6.2 Блок «Пора позаботиться» на главной, настроение питомца
- [x] 6.3 Браузерные уведомления: разрешение, тик 30 c, дедупликация

## Фаза 7 — Статистика
- [x] 7.1 Периоды 7/30/90 дней, карточки-сводки (всего, лапки, среднее/день, серии)
- [x] 7.2 График по дням (столбики с разбивкой по хозяевам)
- [x] 7.3 Разрез по активностям (счет, лапки) и по часам суток
- [x] 7.4 Доля лапок по хозяевам (донат) + таблица регулярности по каждому хозяину

## Фаза 8 — Темы и полировка
- [x] 8.1 Переключатель 5 тем в шапке + сохранение выбора
- [x] 8.2 Пустые состояния, тосты, hover/press-микровзаимодействия, scroll-анимации
- [x] 8.3 Настройки: профиль, export JSON, сброс данных
- [x] 8.4 Продакшен-сборка `npm run build`

## Фаза 9 — Дорожная карта (после MVP)
- [x] 9.1a SQL-миграция: таблицы, RLS, анти-чит триггер лимитов, `claim_invite()` → `supabase/migrations/001_init.sql`
- [x] 9.1b Адаптер `src/lib/cloud.ts`: подключение, e-mail/Google-авторизация, push/pull снапшота
- [x] 9.1c Панель «Облако и синхронизация» в настройках: статус, вход, синк с подтверждением, инструкция
- [ ] 9.1d Боевой запуск: создать проект Supabase, накатить миграцию, вписать URL/ключ, включить Google-провайдера (делается на проде)
- [x] 9.2a PWA-манифест: имя, иконка SVG, цвета night-темы, шорткаты Журнал/Дуэль/Статистика через `?tab=`
- [x] 9.2b Service worker: network-first навигация, stale-while-revalidate статики и шрифтов, офлайн-запуск
- [x] 9.2c Установка на устройство: `beforeinstallprompt` → кнопка «Установить» в шапке, `appinstalled`, iOS-мета
- [x] 9.2d Офлайн-режим: индикатор в шапке + тост; данные local-first, очередь не нужна
- [x] 9.3a Несколько питомцев: `userPets`, активный питомец на вкладку, переключатель в шапке, форма `PetForm` (общая с онбордингом), модалка «Новый питомец», второй питомец в демо
- [x] 9.3b Чат хозяев: `ChatMessage` в БД, `sendMessage`, «Перепалка хозяев» на экране «Дуэль» (пузыри в цветах хозяев, автоскролл, Enter)
- [x] 9.3c Фото к записям: `fileToPhoto` (640 px), прикрепление в модалке отметки, миниатюра в таймлайне журнала, лайтбокс
- [x] 9.3d Облачная схема: миграция `002_chat_photos.sql` (chat_messages + RLS, logs.img)
- [x] 9.4a Вет-календарь: `VetEvent` (прививка/таблетки/ветеринар/осмотр/другое), повторы месяц/год, экран «Здоровье» — сетка месяца с точками, «Ближайшее» со сроками, CRUD-модалка, демо-события
- [x] 9.4b Бейдж навигации: количество событий «на сегодня/просрочено» на вкладке «Здоровье»
- [x] 9.4c Telegram: `lib/telegram.ts` (getMe/sendMessage, русские ошибки), карточка в настройках (токен, chat id, тест, чекбоксы), сводное сообщение раз в 30 c с дедупликацией
- [x] 9.4d Единый тик напоминаний: браузерные уведомления + Telegram для активностей и вет-событий

## Фаза 10 — Настоящее облако
- [x] 10.1 Realtime-синхронизация журнала и чата между устройствами (Supabase Realtime: `logs`, `chat_messages`, события)
  - [x] миграция `003_realtime.sql`: text-иды без FK, `cloud_access` (RLS-участие), `vet_events`, `mirror_upsert_logs()` (история в обход анти-чита), `claim_invite(text)`, подключение к публикации `supabase_realtime`
  - [x] `lib/cloud.ts`: `cloudFullPush` (зеркало pets/acts/access/logs/chat/events + снапшот), `cloudSendDiff` (живая досылка на каждый commit), `cloudClaimInvite` + `cloudFetchPetBundle` (вход по коду между устройствами), `cloudFetchDisplays`, `subscribeRealtime`
  - [x] `AppContext`: облачная сессия и `cloudId`, подписка с дедупликацией по id, «тени» удалённых хозяев (имя/цвет из `cloud_access`, автодобавление в `ownerIds`), тост «прилетело в журнал», статус `rtStatus`
  - [x] Настройки: Realtime-индикатор (онлайн/подключение/офлайн), «Отправить в облако» = полное зеркало со счётчиками, код приглашения работает между устройствами
- [x] 10.2 Фото в Supabase Storage вместо base64: загрузка при отметке, ссылка в `logs.img`, чистка при удалении
  - [x] миграция `004_storage.sql`: бакет `pet-photos`, политики RLS (read: `is_pet_member`, write: владелец + `is_pet_member`, delete: владелец)
  - [x] `lib/cloud.ts`: `cloudUploadPhoto` (загрузка файла → публичный URL), `cloudDeletePhotoUrls` (массовое удаление), `cloudUpsertLogRow` (обновление `logs.img`), `isStorageUrl` (проверка URL)
  - [x] `AppContext`: `complete()` — запись сразу получает id, фото досылается в Storage фоном; `deleteAct()` — удаляет фото из Storage; `resetAll()` — удаляет все фото перед сбросом
  - [x] Настройки: индикатор статуса Storage (готов/не готов), подсказка в модалке отметки «фото загрузится в облако»
- [x] 10.3 Построчная синхронизация `pets`/`pet_owners`/`activity_defs`/`logs`/`chat` поверх снапшотов (снапшот — резервная копия)
  - [x] `lib/cloud.ts`: `cloudRowFetch(meCloudId)` (постраничная выборка всех строк), `mergeRemoteRows()` (идемпотентное слияние по id), мапперы `rowToPet/Act/Log/Chat/Event`
  - [x] `AppContext`: `syncFromCloud()` (скачать → слить → коммит → дослать обратно), `applyMerge()` (повторное слияние свежих данных после диалога)
  - [x] Настройки: кнопка «Синхронизировать» (двухфазная: проверка → merge-диалог), кнопка «Снапшот…» (резервное восстановление)
- [x] 10.4 Гигиена снапшотов: вырезать `pass`/`e-mail` и чувствительные поля перед push
  - [x] `lib/cloud.ts`: `sanitizeForCloud(db)` (удаляет `pass`, `email`, `cloudId` из пользователей, нормализует старые снапшоты), применение в `cloudPush` и `cloudFullPush`
  - [x] `AppContext`: авто-вход по `cloudId` при запуске (один раз за загрузку), `replaceDb()` — вход по облачной связке при восстановлении снапшота
  - [x] Документация: раздел безопасности в README
- [x] 10.5 Разрешение конфликтов: детекция расхождений, merge-диалог («в облаке на N записей больше — объединить?»), outbox-очередь операций
  - [x] `lib/cloud.ts`: `computeDivergence(local, remote, meLocalId)` (двусторонняя разница по id: `incoming`/`outgoing`), `enqueueOutbox` (с дедупликацией и cap 100), `flushOutbox` (отправляет накопленное, успешные удаляет), `outboxCount`, `clearOutbox`; `cloudSendDiff` возвращает `boolean`
  - [x] `AppContext`: `sendDiff` — при офлайне или ошибке дифф уходит в outbox; `fetchDivergence()` (скачать → отложить → вернуть расхождения); `applyMerge()` (повторно мерджит свежие данные, коммитит, делает идемпотентное зеркало + разбор очереди); `flushOutboxNow()` + `outboxN`; авто-разбор при возврате сети и подключении Realtime
  - [x] Настройки: «Синхронизировать» — двухфазный (проверка → merge-диалог с двумя панелями «Из облака — к вам» / «От вас — в облако»); строка outbox (счётчик + кнопка «Отправить»)
- [x] 10.6 Миграции локальной схемы: `db.v` + реестр апгрейдов вместо ad-hoc проверок в `loadDB`
  - [x] `SCHEMA_VERSION` в `lib/types.ts` (сейчас = 5); реестр `MIGRATIONS` в `lib/db.ts` (идемпотентные апгрейды v1→v2 чат, v2→v3 события, v3→v4 вес, v4→v5 расходы), `migrateDB()` накатывает цепочку
  - [x] `loadDB()` мигрирует и пересохраняет новую версию; `emptyDB()`; `ensureDemo`/`buildDemoDB` пишут актуальную версию; `cloudPull` использует `migrateDB` для снапшотов старых версий
- [x] 10.7 Защита квоты localStorage: graceful `QuotaExceeded` (тост, запись без фото), автоочистка самых старых фото
  - [x] `lib/db.ts`: `trimPhotos(db, maxToRemove)` (удаляет фото из самых старых записей), `saveDB()` — при `QuotaExceededError` удаляет до 50 старых фото, если не помогло — ещё 100, возвращает количество удалённых
  - [x] `AppContext`: `commit()` и `replaceDb()` показывают тост при автоочистке

## Фаза 11 — Продакшен-инфраструктура
- [x] 11.1 CI/CD GitHub Actions: lint → tsc → build → деплой (Cloudflare Pages / Netlify или Docker-образ в registry)
  - [x] `.github/workflows/deploy.yml` — триггеры push/PR в main/master, job `build` (typecheck + build + upload artifacts), три варианта деплоя: Cloudflare Pages (основной), Netlify (альтернатива через `vars.DEPLOY_PLATFORM`), Docker (сборка и пуш образа)
  - [x] `Dockerfile` — multi-stage (node:20-alpine → nginx:1.27-alpine), health-check, аргументы для env
  - [x] `docker/nginx.conf` — SPA-роутинг, кэш ассетов 1 год, SW/manifest без кэша, gzip, security headers
  - [x] `.dockerignore`, `.env.example` — оптимизация сборки и документация переменных
- [x] 11.2 Предзаполненное облако из env (`VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY`); панель ручного подключения — только для своих проектов
  - [x] `lib/cloud.ts`: `loadCloudConfig()` проверяет env первым, потом localStorage; `isEnvCloudConfigured()` для UI
  - [x] `Settings.tsx`: если env задан — показываем только форму входа (без полей URL/key), упрощённая инструкция, кнопка "Выйти из облака" вместо "Отключить"
  - [x] `.env.example` с описанием, README с двумя вариантами (A — env для прода, B — ручное для dev)
- [x] 11.3 Мониторинг: Sentry для клиентских ошибок + приватная аналитика (Plausible), ключевая метрика — отметки в день
  - [x] Установлен `@sentry/react`, создан модуль `src/lib/monitoring.ts`
  - [x] Инициализация Sentry и Plausible при запуске (только в продакшене)
  - [x] Трекинг событий: `trackActivityComplete`, `trackPhotoAttached`, `trackPetCreated`, `trackSync`, `trackInviteUsed`, `trackThemeChange`
  - [x] Управление контекстом: `setUserContext`, `clearUserContext`
  - [x] Захват ошибок: `captureError` с контекстом
  - [x] UI для ошибок: `subscribeErrors`, `getRecentErrors` (последние 10 ошибок)
  - [x] Интеграция в ErrorBoundary и AppContext
  - [x] Компонент `MonitoringCard` в настройках с переключателями и списком ошибок
- [x] 11.4 Web Push (VAPID): уведомления при закрытой вкладке через service worker
  - [x] Обновлён `public/sw.js` — добавлены обработчики `push`, `notificationclick`, `notificationclose`
  - [x] Создан `src/lib/web-push.ts` — API для подписки/отписки, проверки поддержки, тестовых уведомлений
  - [x] Созданы Edge Functions: `push-subscribe` (приём subscription), `push-send` (отправка уведомлений)
  - [x] Создана миграция `005_push_subscriptions.sql` — таблица subscriptions с RLS и индексами
  - [x] Добавлен компонент `WebPushCard` в настройки — UI для включения/отключения push
  - [x] Добавлены переменные окружения: `VITE_VAPID_PUBLIC_KEY`, `VITE_SUPABASE_FUNCTIONS_URL`
  - [x] Создана документация `supabase/functions/README.md` с инструкциями по настройке VAPID
- [x] 11.5 Серверные напоминания: Edge Function + pg_cron — ежечасный сбор due-активностей и вет-событий, сводка в Telegram/push без открытого браузера
  - [x] Создана Edge Function `reminders-send` — сбор due-активностей из sync_snapshots, due вет-событий из vet_events, отправка в Telegram и Web Push
  - [x] Создана миграция `006_pg_cron_reminders.sql` — настройка pg_cron для ежечасного запуска (платный план Supabase)
  - [x] Создан GitHub Actions workflow `reminders-cron.yml` — альтернатива для бесплатного плана
  - [x] Создан скрипт `external-cron.sh` — для системного cron
  - [x] Обновлена документация `supabase/functions/README.md` — раздел "Серверные напоминания"
- [x] 11.6 Тесты: unit для доменной логики (лимиты, серии, `computeDue`, повторы вет-событий — включая 31-е число) + Playwright-сценарий «регистрация → отметка → дуэль»
  - [x] Установлены `vitest`, `@vitest/ui`, `@playwright/test`
  - [x] Создан `vitest.config.ts` с окружением jsdom
  - [x] Создан `src/test/domain.test.ts` — тесты для `limitsFor`, `streakDays`, `computeDue`, `nextOccurrence` (включая 31-е число в феврале)
  - [x] Создан `playwright.config.ts` с Chromium
  - [x] Создан `e2e/full-scenario.spec.ts` — полный сценарий и демо-режим
  - [x] Добавлены скрипты в `package.json`: `test`, `test:ui`, `test:coverage`, `test:e2e`, `test:e2e:ui`
  - [x] Создан `TESTING.md` — полная документация по тестированию

## Фаза 12 — Здоровье+
- [x] 12.1 Ветпаспорт: выгрузка истории прививок/обработок/осмотров в PDF с QR-кодом для клиники
  - [x] Установлены `jspdf` и `qrcode`
  - [x] Создан модуль `src/lib/vet-passport.ts` с функцией `generateVetPassport()`
  - [x] Генерация PDF с информацией о питомце, QR-кодом, статистикой активностей, вет-событиями
  - [x] Кнопка "Ветпаспорт" на экране "Здоровье" (VetScreen)
  - [x] Автоматическое скачивание PDF с именем `vet-passport-{имя-питомца}-{timestamp}.pdf`
- [x] 12.2 Автографик прививок по виду и возрасту: серия событий при создании питомца (8/12 недель, ежегодные бустеры)
  - [x] Создан модуль `src/lib/vet-schedule.ts` с графиками прививок для 6 видов животных
  - [x] Графики для кошек, собак, кроликов, попугаев, хомяков, рыбок
  - [x] Функция `generateVetEventsForPet()` генерирует события на основе вида и возраста
  - [x] Интеграция в `createPet()` — автоматическое создание графика при добавлении питомца
  - [x] Функция `regenerateVetSchedule()` для обновления графика
  - [x] Кнопка "Обновить график" на экране "Здоровье" с модалкой подтверждения
- [x] 12.3 Вес и замеры: кривая веса по времени, расчёт дозировки препаратов от глистов по весу
  - [x] Добавлены типы `WeightEntry` и поле `weights` в DB (SCHEMA_VERSION = 4)
  - [x] Создан модуль `src/lib/weight.ts` с функциями: расчёт дозировок, оценка веса, график
  - [x] Добавлены функции `addWeight`, `updateWeight`, `deleteWeight` в AppContext
  - [x] Создан компонент `WeightTracker` с графиком, историей, оценкой веса и расчётом дозировок
  - [x] Интеграция в экран "Здоровье" (VetScreen)
  - [x] Демо-данные веса для Булки и Кеши
  - [x] Миграция v3 → v4 для добавления поля weights

## Фаза 13 — Удержание
- [x] 13.1 Ачивки и бейджи («7 дней серии», «первая сотня лапок», «все заботы за день»), недельные челленджи хозяев
  - [x] Создан модуль `src/lib/achievements.ts` с 15 ачивками 4 уровней редкости
  - [x] Ачивки: обычные (5), редкие (5), эпические (3), легендарные (2)
  - [x] 5 еженедельных челленджей с прогрессом
  - [x] Создан компонент `AchievementsPanel` с вкладками ачивок и челленджей
  - [x] Интеграция в главное меню (новая вкладка "Ачивки")
  - [x] Статистика по редкостям, прогресс-бары, визуальная индикация
- [x] 13.2 Цели и план на день: чек-лист забот с прогресс-кольцом
  - [x] Создан модуль `src/lib/daily-goals.ts` с генерацией ежедневных целей
  - [x] Приоритизация активностей (кормление, вода, лоток в приоритете)
  - [x] Автоматический расчет целевого количества на основе remindH и лимитов
  - [x] Отслеживание прогресса выполнения в реальном времени
  - [x] Создан компонент `DailyGoals` с прогресс-кольцом и чек-листом
  - [x] Визуальная индикация выполненных целей (зеленая галочка, зачеркивание)
  - [x] Интеграция в HomeScreen между напоминаниями и быстрыми активностями
- [x] 13.3 Учёт расходов: корм/наполнитель/ветеринар, суммы по категориям и месяцам
  - [x] Добавлены типы `Expense`, `ExpenseCategory` и поле `expenses` в DB (SCHEMA_VERSION = 5)
  - [x] Создан модуль `src/lib/expenses.ts` с функциями: категории расходов, подсчёт сумм, форматирование
  - [x] 7 категорий: корм, наполнитель, ветеринар, игрушки, аксессуары, груминг, другое
  - [x] Добавлены функции `addExpense`, `updateExpense`, `deleteExpense` в AppContext
  - [x] Создан компонент `ExpensesTracker` с графиком по месяцам, разбивкой по категориям, списком расходов
  - [x] Навигация по месяцам, модалка добавления/редактирования расхода
  - [x] Интеграция в экран "Здоровье" (VetScreen)
  - [x] Миграция v4 → v5 для добавления поля expenses
- [x] 13.4 Роли и «от имени»: помощники без права редактировать активности, отметка «сделал за …»
  - [x] Добавлены типы `UserRole` ("owner" | "helper") и поле `ownerRoles` в Pet
  - [x] Добавлено поле `onBehalfOf` в LogEntry для отметок "от имени"
  - [x] Добавлены функции `getUserRole`, `setUserRole`, `canEditActivities` в AppContext
  - [x] Обновлена функция `complete` для поддержки параметра `onBehalfOf`
  - [x] UI выбора "от имени" в модалке отметки активности (HomeScreen)
  - [x] UI управления ролями в настройках (Settings) с кнопками переключения ролей
  - [x] Ограничение редактирования активностей для помощников (скрыты кнопки редактирования/удаления)
  - [x] Отображение отметок "от имени" в журнале (Journal) с индикатором "→ имя"
  - [x] Добавлена иконка "user" в icons.tsx
- [x] 13.5 Полировка: авто-тема по системной, i18n (ru/en), a11y-аудит (клавиатура, aria, контраст), онбординг-тур, карточка «30 дней заботы» для соцсетей
  - [x] Создана система i18n с поддержкой русского и английского языков (`src/lib/i18n.ts`)
  - [x] Создан хук `useI18n` для использования переводов в компонентах
  - [x] Добавлен переключатель языка в настройках
  - [x] Реализована авто-тема по системной (следит за `prefers-color-scheme`)
  - [x] Создан компонент онбординг-тура для новых пользователей (`OnboardingTour.tsx`)
  - [x] Создан компонент карточки «30 дней заботы» для шеринга в соцсетях (`SocialShareCard.tsx`)
  - [x] Добавлены новые иконки: globe, share
  - [x] Интегрированы все компоненты в основное приложение

## Фаза 14 — Сезоны и чемпионы
- [x] 14.1 Настройка «Обнулять лапки ежемесячно» (включена по умолчанию): семантика сезона — данные не удаляются, баланс сезона считается с 1-го числа; уровень заботы остаётся пожизненным, сезонный баланс движет «Дуэлью» и карточкой «Сегодня»
  - [x] Добавлены типы `SeasonSettings` и `MonthlyResult` в types.ts
  - [x] Создан модуль `src/lib/seasons.ts` с функциями для работы с сезонами
  - [x] Добавлены поля `seasonSettings` и `monthlyResults` в Pet
  - [x] Реализованы функции: `getSeasonSettings`, `getSeasonStart`, `getSeasonEnd`, `getSeasonLogs`, `getSeasonPawsByUser`, `getSeasonActivitiesByUser`, `getSeasonWinner`, `getSeasonInfo`, `finalizeMonth`, `getMonthlyResults`, `getYearlyChampion`
  - [x] Добавлены функции в AppContext: `updateSeasonSettings`, `seasonLogs`, `seasonPawsByUser`, `seasonActivitiesByUser`, `seasonWinner`, `seasonInfo`, `monthlyResults`, `yearlyChampion`, `finalizeCurrentMonth`
  - [x] Обновлён HomeScreen для отображения сезонного баланса
  - [x] Обновлён DuelScreen для поддержки сезонного периода и отображения истории победителей
  - [x] Добавлена секция настройки сезонного обнуления в SettingsScreen
- [x] 14.2 Сезонная механика: чип «Сезон июля · осталось N дней» на «Сегодня» и «Дуэли»; при выключенной настройке — текущее поведение (счёт с самого начала)
  - [x] Добавлен чип с информацией о сезоне на экран «Сегодня» (HomeScreen)
  - [x] Добавлен чип с информацией о сезоне на экран «Дуэль» (DuelScreen)
  - [x] Чип показывает название месяца и количество дней до конца сезона
  - [x] При выключенной настройке сезонного обнуления чип не отображается
  - [x] При выключенной настройке используется обычное поведение (счёт с самого начала)
  - [x] Уровень заботы всегда считается по общему количеству лапок (независимо от настройки)
- [x] 14.3 Закрытие месяца: `MonthlyResult { petId, month, rows[{ownerId, paws, count}], winnerId | null }` — автоподведение при первом открытии нового месяца + кнопка «Подвести месяц вручную»; ничья: лапки → число активностей → «ничья»
  - [x] Реализована структура MonthlyResult с полями petId, month, winnerId, pawsByUser, activitiesByUser
  - [x] Создана логика определения победителя: сначала по количеству лапок, при их равенстве - по количеству активностей, при равенстве активностей - ничья (winnerId = null)
  - [x] Добавлено автоматическое закрытие месяца при первом открытии нового месяца через функцию autoFinalizePreviousMonth()
  - [x] Реализована кнопка "Подвести месяц вручную" в настройках, доступная в любой момент
  - [x] Уведомление о закрытии месяца с указанием победителя или ничьей
- [x] 14.4 «Кубок» на экране «Дуэль»: история победителей по месяцам (корона, лапки, активности), текущий сезон «идёт», чемпион года — по числу месячных побед («Алина — 7 из 12 месяцев»)
  - [x] Создан компонент `Trophy` в `src/components/Trophy.tsx`
  - [x] Отображение истории победителей по месяцам с иконками короны
  - [x] Показ текущего сезона с пометкой «идёт» и пульсирующей иконкой трофея
  - [x] Блок «Чемпион года» с аватаром, короной и статистикой побед
  - [x] Для каждого месяца: имя победителя, количество лапок и активностей
  - [x] Интеграция компонента в экран «Дуэль»
- [x] 14.5 Краевые случаи: хозяин вышел из питомца посреди сезона (сохраняем результат с пометкой), питомец создан в середине месяца (сезон с даты создания)
  - [x] **Хозяин вышел из питомца посреди сезона**: сохраняем результат с пометкой о бывших хозяевах
    - В `finalizeMonth()` проверяем, есть ли пользователи в pawsByUser, которые больше не в ownerIds
    - Добавляем поле `note` в MonthlyResult с информацией о бывших хозяевах
    - Результат сохраняется, но с пометкой для прозрачности
  - [x] **Питомец создан в середине месяца**: сезон начинается с даты создания
    - В `getSeasonStart()` сравниваем дату создания питомца с началом сезона
    - Если питомец создан после начала сезона, сезон начинается с `pet.createdAt`
    - В `getSeasonEnd()` учитываем этот случай для корректного расчёта конца сезона
    - Логи до создания питомца автоматически исключаются из сезонной статистики

## Итого

**Всего задач:** 145
**Выполнено:** 144
**Осталось:** 1 (9.1d — боевой запуск Supabase, делается на проде)

**Прогресс:** 99.3%
